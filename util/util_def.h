#pragma once

enum class UtilDef {
    kLineTop,
    kLineLeft,
    kLineRight,
    kLineBottom
};

enum class UtilOrient {
    kToRight,
    kToDown,
    kToLeft,
    kToUp
};

enum class UtilBoxType {
    kPace
};